<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Get CRUD</title>
</head>

<style>
    * {
        text-align: center;
    }

    table,
    th,
    td {
        border: 1px solid black;
        border-collapse: collapse;
    }


    .btn-edit {
        background-color: orange;
        border: none;
        border-radius: 4px;
        height: 25px;
        width: 60px;
        margin: 5px;
    }

    .btn-delete {
        background-color: #fc005e;
        border: none;
        border-radius: 4px;
        height: 25px;
        width: 60px;
        margin: 5px;
    }
</style>

<body>
    <div className="container">
        <div className="content">
            <table style="width: 100%">
                <tr>
                    <th>First Name</th>
                    <th>Last Name</th>
                    <th>Address</th>
                    <th>Points</th>
                    <th>Leader</th>
                    <th>Previous</th>
                    <th>HN</th>
                    <th>DN</th>
                    <th>HCM</th>
                    <th>ND</th>
                    <th>Action</th>
                </tr>
                <% for(let i=0; i < dataTable.length; i++) {%>
                    <tr>
                        <td>
                            <%= dataTable[i].firstName %>
                        </td>
                        <td>
                            <%= dataTable[i].lastName %>
                        </td>
                        <td>
                            <%= dataTable[i].address %>
                        </td>
                        <td>
                            <%= dataTable[i].haNoi + dataTable[i].hoChiMinh + dataTable[i].daNang + dataTable[i].namDinh
                                %>
                        </td>
                        <td>
                            <%= (dataTable[0].haNoi + dataTable[0].hoChiMinh + dataTable[0].daNang +
                                dataTable[0].namDinh) - (dataTable[i].haNoi + dataTable[i].hoChiMinh +
                                dataTable[i].daNang + dataTable[i].namDinh) %>
                        </td>
                        <td>
                            <% if (i> 0) { %>
                                <%= (dataTable[i-1].haNoi + dataTable[i-1].hoChiMinh + dataTable[i-1].daNang +
                                    dataTable[i-1].namDinh) - (dataTable[i].haNoi + dataTable[i].hoChiMinh +
                                    dataTable[i].daNang + dataTable[i].namDinh) %>
                                    <% } %>
                        </td>
                        <td>
                            <%= dataTable[i].haNoi %>
                        </td>
                        <td>
                            <%= dataTable[i].hoChiMinh %>
                        </td>
                        <td>
                            <%= dataTable[i].daNang %>
                        </td>
                        <td>
                            <%= dataTable[i].namDinh %>
                        </td>
                        <td>
                            <a href="/edit-crud?id=<%=dataTable[i].id%>" class="btn-edit" type="button">Edit</button>
                                <a href="/delete-crud?id=<%=dataTable[i].id%>" class="btn-delete"
                                    type="button">Delete</button>
                        </td>
                    </tr>
                    <%}%>
            </table>
        </div>
    </div>
</body>

</html>